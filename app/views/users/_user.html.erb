<li class="list-group-item list-group-item-action ">
	<%= link_to gravatar_for(user),user, size: 50 %>
	<%= link_to user.name, user,class: "UserInfoLink"%>
	<% if current_user.admin? && !current_user?(user) %>
	 <%= link_to "Delete", user, method: :delete, data: { confirm: "You sure" },class: "btn btn-danger UserDeleteButton "%>
	<% end %>
</li>